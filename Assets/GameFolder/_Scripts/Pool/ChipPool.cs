using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace SKC.DeterministicRoulette.Pool
{
    [CreateAssetMenu(menuName = nameof(DeterministicRoulette) + "/" + nameof(Pool) + "/" + nameof(ChipPool))]
    public class ChipPool : Pool<PoolObject>
    {

    }
}
